#include <iostream>
#include<vector>
#include<algorithm>
#include<numeric>
#include<map>
#include <bits/stdc++.h> 
#include <cmath>   
#include <unordered_set>
#include <iostream>     
#include <algorithm>  
#include <vector>
#include <stdio.h>
#include <ctype.h>
using namespace std;
void calculate(vector<int> nums){
    int evens = 0;
    int odds = 0;
    int ans = 0;
    int maxSum = 0;
    minOdd = 
    for(int i = 0;i<nums.size();i++){
        if(nums[i]%2 == 0){
            evens++;
        } else {
            odds++;
        }
        maxSum+=nums[i];
    }
    if(odds != 0){
        if(odds)
    }
}

int main(){
    int i;
    cin >> i;
    vector<int> nums;
    for(int j =0;j<i;j++){
        int l;
        cin >> l;
        nums.push_back(l);
    }
    calculate(nums);
}   