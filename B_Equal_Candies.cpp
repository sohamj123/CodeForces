#include <iostream>
#include<vector>
#include<algorithm>
#include<numeric>
#include<map>
#include <bits/stdc++.h> 
#include <cmath>   
#include <unordered_set>
#include <iostream>     
#include <algorithm>  
#include <vector>
#include <stdio.h>
#include <ctype.h>
#include <iostream>     
#include <algorithm>    
#include <vector>
using namespace std;
void calculate(int l, vector<int> nums){
    int sum = 0;
    int min = -1;
    for(int i = 0;i<l;i++){
        if
    }
}

int main(){
    int x;
    cin >> x;
    for(int i = 0;i<x;i++){
        int c;
        cin >> c;
        vector<int> nums;
        for(int i = 0;i<c;i++){
            int z;
            cin >> z;
            nums.push_back(z);
        }
        calculate(c,nums);
    }
        
}   